// Analyses 
// Declare two functions: 
// calculateRectangleArea
// calculateRectanglePerimeter
// Declare two functions:
// calculateCircleArea
// calculateCircleCircumference
// Call the rectangle functions: length and width values
//  Call the circle functions: radius values
// Print the calculated area and perimeter (circumference) for each shape, indicating which calculation was performed.
function calculateRectangleArea(length, width) 
{
   if(isNaN (length) || isNaN(width)) {
    return;}
    return length, width;
}
function calculateRectanglePerimeter(length, width) 
{
   if(isNaN (length) || isNaN(width)) {
    return;}
    return length, width;
}
const calculateRectangleArea = (lenght, width) ==>
{
    if(isNaN (length) || isNaN(width)) {
        return;}
}
const calculateRectanglePerimeter = (lenght, width) ==>
{
    if(isNaN (length) || isNaN(width))
}